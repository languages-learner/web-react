{
  "name": "@languages-learner/api",
  "version": "1.0.0",
  "private": true,
  "description": "Supabase and PostgREST client helpers and database type generation for Languages Learner.",
  "main": "src/index.ts",
  "scripts": {
    "lint": "eslint --concurrency=auto .",
    "lint:fix": "npm run lint -- --fix",
    "typecheck": "tsc --noEmit",
    "generate-types": "ts-node --project ./scripts/tsconfig.json ./scripts/generate-db-types.ts"
  },
  "dependencies": {
    "@supabase/postgrest-js": "1.19.4",
    "@supabase/supabase-js": "2.49.4"
  },
  "devDependencies": {
    "dotenv": "^16.5.0"
  }
}
