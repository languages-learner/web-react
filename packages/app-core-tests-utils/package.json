{
  "name": "@languages-learner/app-core-tests-utils",
  "version": "1.0.0",
  "private": true,
  "description": "Shared utilities for app-level tests.",
  "main": "src/index.ts",
  "scripts": {
    "lint": "eslint --concurrency=auto .",
    "lint:fix": "npm run lint -- --fix",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@playwright/test": "1.52.0",
    "@languages-learner/playwright-utils": "workspace:*"
  }
}
